<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      One permission, and only since the app learned to hand a finished file to
      OctoPrint. The page itself still cannot reach the network: every request
      it makes is either answered from inside the APK or refused outright in
      MainActivity. The single exception is the upload, which is performed by
      the app rather than the page, to the address the user typed and nowhere
      else. Models never leave the device; a G-code file goes to your printer
      when you press the button.

      Cleartext is allowed because OctoPrint on a home network is http, and a
      printer on the far side of a router is not a thing this app can fix.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.WebSlicer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
